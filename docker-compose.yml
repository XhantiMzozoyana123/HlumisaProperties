services:
  hlumisa-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "6000:6000"
    env_file:
      - .env
    environment:
      - ASPNETCORE_URLS=http://0.0.0.0:6000
    restart: always
