services:
  hlumisa-api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000" 
    env_file:
      - .env
    environment:
      - ASPNETCORE_URLS=http://0.0.0.0:5000
    restart: always
